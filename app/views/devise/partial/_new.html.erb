<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
    <h3 id="myModalLabel">Entrar</h3>
  </div>
<div class="modal-body">
   <%= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>

  <div>
  <%= f.text_field :login, :autofocus => true, placeholder: "E-mail ou Usuário" %></div>
  <div>
  <%= f.password_field :password, placeholder: "Senha" %></div>

  <% if devise_mapping.rememberable? -%>
    <div><%= f.check_box :remember_me %> <%= f.label "Lembrar-me" %></div>
  <% end -%>
      <%= render "devise/shared/links" %>
 </div>     
 <div class="modal-footer">
    <div><%= f.submit "Entrar" ,class:"btn btn-warning"%></div>
  <% end %>
</div>

