<%= form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f| %>
  <%= devise_error_messages! %>
  <div>
  	<%= f.text_field :username, placeholder: "Usuário"%>
  </div>
  <div>
  <%= f.email_field :email, :autofocus => true, placeholder: "E-mail" %></div>
  <div>
  <%= f.password_field :password, placeholder: "Senha" %></div>
  <div>
  <%= f.password_field :password_confirmation, placeholder: "Confirmar senha" %></div>
  <div><%= f.submit "Entrar", class:"btn btn-warning" %></div>
<% end %>

<%= render "devise/shared/links" %>
